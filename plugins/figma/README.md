# Figma plugin

This is a lightweight scaffold for tracking a Figma integration in this repo.

## Auth

Set `FIGMA_ACCESS_TOKEN` in your environment (a Figma Personal Access Token).

## Next steps (implementation ideas)

- Add a small CLI or library wrapper that uses the Figma REST API.
- Store any code under `plugins/figma/src/` and expose a stable entrypoint.
